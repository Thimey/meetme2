<h1>Welcome</h1>

<table>
<tr>
<th>Name</th>
<th>Name</th>
<th></th>
<th></th>
</tr>
<%= @users.each do |user| %>
	<tr>
		<td><%= link_to user.name %></td>
		<td><%= user.email %></td>
		<td><%= link_to 'Edit', edit_user_path %></td>
		<td><%= link_to 'Delete', edit_user_path %></td>
	</tr>
<% end %>
</table>

<%= link_to 'Add a user', new_user_path(user) %>
<%= link_to ' See all users', users_path %>